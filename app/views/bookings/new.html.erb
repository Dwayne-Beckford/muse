<div class="container col-6 mt-5">
<h1>New Booking</h1>
<%= render partial: "form", locals: { artwork: @artwork, booking: @booking } %>
<%= link_to 'back', artworks_path(@artwork), class: "btn btn-primary"%>
</div>
