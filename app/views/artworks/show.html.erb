<h2> <%= @artwork.name %></h2>

<%= @artwork.description %>
<%= @artwork.location %>
<%= @artwork.price %>

<%= render partial: "bookings/form", locals: { artwork: @artwork, booking: @booking } %>

<%= link_to 'back', artworks_path, class: "btn btn-primary"%>

